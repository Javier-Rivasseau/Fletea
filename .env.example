# === Configuración del Servidor ===
PORT=3000
NODE_ENV=development
# 'simulation' = sin WhatsApp real | 'production' = con WhatsApp real
MODE=simulation

# === Base de Datos (Supabase PostgreSQL) ===
# Obtené tu URL desde: Supabase Dashboard > Settings > Database > Connection String > URI
DATABASE_URL=postgresql://postgres.TU_USUARIO:TU_PASSWORD@aws-0-us-east-1.pooler.supabase.com:6543/postgres

# === WhatsApp (Baileys) ===
# Setear en 'true' para activar el bot de WhatsApp y ver el QR en el dashboard
# No hace falta número de teléfono — se escanea el QR con la app de WhatsApp
ENABLE_WHATSAPP=true
# PHONE_NUMBER=5492314... (opcional: usar pairing code en vez de QR)

# === IA (Kimi 2.5 vía NVIDIA NIM) ===
# Obtené tu API key en: https://integrate.api.nvidia.com
KIMI_API_KEY=nvapi-...
KIMI_BASE_URL=https://integrate.api.nvidia.com/v1
KIMI_MODEL=moonshotai/kimi-k2.5

# === ZEABUR — Variables a configurar en el dashboard de Zeabur ===
# (Zeabur no lee el .env, hay que cargarlas manualmente en: Service > Variables)
#
# DATABASE_URL=...
# ENABLE_WHATSAPP=true
# MODE=production
# KIMI_API_KEY=...
# KIMI_BASE_URL=https://integrate.api.nvidia.com/v1
# KIMI_MODEL=moonshotai/kimi-k2.5
# NODE_ENV=production
